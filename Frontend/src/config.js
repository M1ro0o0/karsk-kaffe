// Single source of truth for the backend URL.
// Change this one value if your backend URL ever changes —
// every API call across the app will follow automatically.
 
export const API_URL = "https://karsk-kaffe.onrender.com";
 